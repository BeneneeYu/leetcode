# Core

Non-comparison integer sorting algorithm, split an integer and compare every bit. Cause integer can represent string and float in specific format.

# Comparison

Radix: Assign buckets according to every digit of key

Counting: Every bucket stores a single value

Bucket: Every bucket stores a range of values

# Procedure

From low digit to high digit, compare and sort recursively.



